@import '~styles/variables'

.toast
	position: absolute
	bottom: 0
	left: $shell-padding * 2
	right: @left
	padding: 7px 10px
	z-index: 1000

	&-close
		float: right
		margin-left: 10px

	&-error
		a:hover
			color: $black
